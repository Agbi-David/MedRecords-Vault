import InstitutionSignupForm from '@/components/auth/InstitutionSignupForm';

export default function InstitutionSignupPage() {
    return (
        <div className="min-h-screen bg-gradient-to-b from-green-100 to-white flex items-center justify-center p-4">
            <InstitutionSignupForm />
        </div>
    );
}
